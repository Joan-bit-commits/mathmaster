import { useRef, useState } from 'react';
export default function useUpload(uploadFn) { const [progress, setProgress] = useState(0); const [isUploading, setUploading] = useState(false); const [error, setError] = useState(null); const [data, setData] = useState(null); const cancelRef = useRef(null); const upload = async (...args) => { setUploading(true); setError(null); try { const result = await uploadFn(...args, setProgress); setData(result); return result; } catch (err) { setError(err); throw err; } finally { setUploading(false); } }; return { upload, progress, isUploading, error, data, cancel: () => cancelRef.current?.() }; }