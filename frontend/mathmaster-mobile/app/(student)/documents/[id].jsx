import React, { useEffect, useState } from 'react';
import { Pressable, ScrollView, Text, TextInput, View } from 'react-native';
import { router, useLocalSearchParams } from 'expo-router';
import { fetchDocument } from '../../../src/services/documents';
import useDocumentQA from '../../../src/hooks/useDocumentQA';
import Screen from '../../../src/components/ui/Screen';
import ModeTabs from '../../../src/components/ui/ModeTabs';
import QuickPromptChips from '../../../src/components/ui/QuickPromptChips';
import CitationChip from '../../../src/components/ui/CitationChip';
export default function DocumentDetail() { const { id } = useLocalSearchParams(); const [document, setDocument] = useState(null); const [mode, setMode] = useState('Ask'); const [question, setQuestion] = useState(''); const qa = useDocumentQA(id); useEffect(() => { fetchDocument(id).then(setDocument); qa.loadSessions(); }, [id]); if (!document) return <View className="flex-1 bg-background" />; return <Screen className="flex-1 bg-background"><ScrollView className="flex-1 px-6" contentContainerStyle={{ paddingTop: 24, paddingBottom: 30 }}><View className="flex-row items-center gap-3"><Pressable onPress={() => router.back()} accessibilityRole="button" accessibilityLabel="Go back"><Text className="text-[24px] text-on-surface">‹</Text></Pressable><View className="flex-1"><Text numberOfLines={1} className="font-title-lg text-on-surface">{document.title}</Text><Text className="font-body-sm text-on-surface-variant">{document.page_count || '?'} pages</Text></View></View><ModeTabs value={mode} onChange={setMode} options={['Read', 'Ask', 'Sessions']} className="mt-5" />{mode === 'Read' && <View className="mt-5 rounded-2xl bg-surface-container-lowest p-5"><Text className="font-title-lg text-on-surface">Document preview</Text><Text className="font-body-md mt-4 text-on-surface-variant">This document is ready for grounded questions. Open the PDF viewer to read the source pages.</Text></View>}{mode === 'Ask' && <View className="mt-5"><QuickPromptChips prompts={['Summarise this chapter', 'Explain the worked example', 'What mistakes should I avoid?']} onSelect={setQuestion} /><View className="mt-5 gap-3">{qa.messages.map((message, index) => <View key={`${message.role}-${index}`} className={`rounded-2xl p-4 ${message.role === 'user' ? 'ml-8 bg-primary-fixed' : 'mr-8 bg-surface-container-lowest'}`}><Text className="font-body-md text-on-surface">{message.content}</Text></View>)}{qa.isStreaming && <View className="mr-8 rounded-2xl bg-surface-container-lowest p-4"><Text className="font-body-md text-on-surface">{qa.currentAnswer}</Text></View>}{qa.citations.map((citation, index) => <CitationChip key={`${citation.page}-${index}`} page={citation.page} />)}</View><View className="mt-5 flex-row items-end rounded-2xl bg-surface-container-lowest p-2"><TextInput value={question} onChangeText={setQuestion} placeholder="Ask about this document" placeholderTextColor="#6e7881" className="max-h-24 flex-1 px-2 py-2 text-on-surface" multiline accessibilityLabel="Document question" /><Pressable onPress={() => { qa.sendQuestion(question); setQuestion(''); }} className="h-10 w-10 items-center justify-center rounded-full bg-primary" accessibilityRole="button" accessibilityLabel="Send document question"><Text className="text-[20px] text-white">↑</Text></Pressable></View></View>}{mode === 'Sessions' && <View className="mt-5 gap-3">{qa.sessions.map((session) => <Pressable key={session.id} className="rounded-2xl bg-surface-container-lowest p-4" accessibilityRole="button" accessibilityLabel={`Open session ${session.title}`}><Text className="font-title-lg text-on-surface">{session.title || 'Document chat'}</Text></Pressable>)}</View>}</ScrollView></Screen>; }