import React, { useState } from 'react';
import { Pressable, ScrollView, Text, View } from 'react-native';
import { router, useLocalSearchParams } from 'expo-router';
import { useLocalProblems, useObjective, useWorkedExamples } from '../../../src/hooks/useCurriculum';
import CurriculumBreadcrumb from '../../../src/components/ui/CurriculumBreadcrumb';
import WorkedExampleCard from '../../../src/components/ui/WorkedExampleCard';
import UNEBCodeBadge from '../../../src/components/ui/UNEBCodeBadge';
export default function CurriculumTopic() { const { code } = useLocalSearchParams(); const [tab, setTab] = useState('Objectives'); const { data: objective } = useObjective(code); const { data: examples = [] } = useWorkedExamples(code); const { data: problems = [] } = useLocalProblems(code); if (!objective) return <View className="flex-1 bg-background" />; return <ScrollView className="flex-1 bg-background px-6" contentContainerStyle={{ paddingTop: 24, paddingBottom: 40 }}><CurriculumBreadcrumb items={[{ label: objective.level }, { label: objective.subject }, { label: objective.strand }, { label: objective.topics?.[0] || 'Topic' }]} onPress={() => {}} /><View className="mt-6"><UNEBCodeBadge code={objective.code} size="md" /><Text className="font-headline-sm mt-3 text-on-surface">{objective.text}</Text><Text className="font-body-md mt-2 text-on-surface-variant">{objective.difficulty} · {objective.estimated_hours} hours</Text></View><View className="mt-6 flex-row rounded-2xl bg-surface-container p-1">{['Objectives', 'Examples', 'Misconceptions', 'Practice'].map((item) => <Pressable key={item} onPress={() => setTab(item)} className={`flex-1 items-center rounded-xl px-1 py-2 ${tab === item ? 'bg-surface-container-lowest' : ''}`} accessibilityRole="tab"><Text className={`font-label-sm text-center ${tab === item ? 'text-primary' : 'text-on-surface-variant'}`}>{item}</Text></Pressable>)}</View>{tab === 'Objectives' && <View className="mt-5 rounded-2xl bg-surface-container-lowest p-4"><Text className="font-title-lg text-on-surface">What you will learn</Text><Text className="font-body-md mt-3 text-on-surface-variant">{objective.topics?.join(' · ')}</Text><Text className="font-title-lg mt-6 text-on-surface">Textbook reference</Text><Text className="font-body-md mt-2 text-on-surface-variant">{objective.workbook_refs?.[0]?.textbook}, p. {objective.workbook_refs?.[0]?.page}</Text></View>}{tab === 'Examples' && <View className="mt-5 gap-3">{examples.map((example) => <WorkedExampleCard key={example.id} example={example} />)}</View>}{tab === 'Misconceptions' && <View className="mt-5 rounded-2xl bg-surface-container-lowest p-4">{objective.common_misconceptions?.map((item) => <Text key={item} className="font-body-md mb-3 text-on-surface">• {item}</Text>)}</View>}{tab === 'Practice' && <View className="mt-5 gap-3">{problems.map((problem) => <View key={problem.id} className="rounded-2xl bg-surface-container-lowest p-4"><Text className="font-body-md text-on-surface">{problem.problem}</Text><Text className="font-label-sm mt-2 text-primary">{problem.context}</Text></View>)}</View>}<Pressable onPress={() => router.push({ pathname: '/(student)/ai-tutor/chat/1', params: { initial: objective.text } })} className="mt-6 items-center rounded-full bg-primary py-3" accessibilityRole="button" accessibilityLabel="Ask AI tutor about this objective"><Text className="font-label-sm text-on-primary">Ask AI tutor</Text></Pressable></ScrollView>; }